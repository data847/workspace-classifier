# ── Core (Drive scan, Gmail export, S3 upload) ─────────────────
google-api-python-client>=2.100.0
google-auth>=2.20.0
google-auth-httplib2>=0.2.0
google-auth-oauthlib>=1.2.0
python-dotenv>=1.0.0
pandas>=2.0.0
boto3>=1.34.0

# ── LLM classification (not needed for --export-only) ─────────
anthropic>=0.40.0
openai>=1.50.0
tiktoken>=0.7.0

# ── Inventory output (classify mode) ────────────────────────────
openpyxl>=3.1.0

# ── File extractors (classify mode) ─────────────────────────────
pypdf>=4.0.0
python-docx>=1.1.0
python-pptx>=0.6.23
beautifulsoup4>=4.12.0
lxml>=5.0.0
Pillow>=10.0.0
